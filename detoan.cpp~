#include<bits/stdc++.h>
using namespace std ; 

const int N = 1e5 ; 
int f[N] ;
signed main()
{
    int n = 10 ; 
    int res = 0 ; 
    for(int i=0;i<(1<<n);i++)
    {
        if((i&(i<<1))>0)res+=0 ; 
        else res++; 
    }
    cout<<res<<endl; 
}